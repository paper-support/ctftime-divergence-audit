httpx>=0.27
# Phase 3 (writeups) — installed separately via `bash ~/.cc-switch/skills/scrapling/scripts/setup.sh`
# scrapling[all]>=0.4.7
# Phase 4+ (analysis) — installed when needed
# pandas>=2.2
# pyarrow>=15
# scikit-learn>=1.4
# sentence-transformers>=2.7
# bertopic>=0.16
